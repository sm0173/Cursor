# Память агента структуры (самообучение)

Сюда агент **дописывает** короткие выводы после согласованных изменений. Пользователь **не обязан** править инструкцию в `.cursor/rules/` — знание накапливается здесь.

Формат записи:

```
## YYYY-MM-DD — краткий заголовок
- Контекст: что хотели упорядочить.
- Решение: что сделали (пути).
- Вывод: правило на будущее / что не повторять.
```

---

## 2026-04-17 — эталонная структура `Прога/`: `docs/` + `scripts/`
- Контекст: нужно развести **все** элементы зоны: не только `01_`…`03_`, но и README, индекс, конвертацию — по эталону «документация / скрипты / данные» (как в шаблонах курса со `scripts/` и отдельными справочниками).
- Решение: в корне `Прога/` оставлен только `README.md`; добавлены `docs/` (`README.md`, `INDEX.md`), `scripts/` (`convert_materials.py`, `requirements-materials.txt`); в скрипте `ROOT = parent.parent`; обновлены `tasks/project_map.md`, README подпапок материалов.
- Вывод: команды установки и запуска — через `Прога/scripts/…`; каталог — `Прога/docs/INDEX.md`.

## 2026-04-17 — префиксы `01_`…`03_` у папок в `Прога/`
- Контекст: нужна **видимая глазами** упорядоченная структура в проводнике, не только запись в `project_map.md`.
- Решение: папки переименованы в `01_Оригиналы материалов`, `02_Материалы по ИАДу`, `03_Материалы по питону`; обновлены `convert_materials.py`, `Прога/README.md`, `INDEX.md`, под-README, `tasks/project_map.md`.
- Вывод: при ссылках на материалы использовать новые пути; нумерация фиксирует порядок «источник → ИАД → Python».

## 2026-04-17 — индекс `Прога/` в `project_map.md`
- Контекст: элементы зоны `Прога` (3 папки + 4 файла в корне) должны быть явно зафиксированы там, куда смотрит агент структуры в первую очередь.
- Решение: в `tasks/project_map.md` добавлена секция **«Индекс зоны Прога»**; ссылка на каталог — `Прога/docs/INDEX.md` (позже индекс перенесён в `docs/`).
- Вывод: при изменении состава зоны — править секцию индекса в `project_map.md` и `Прога/docs/INDEX.md`.

## 2026-04-17 — INDEX.md в Прога (структура верхнего уровня)
- Контекст: нужен каталог **папок и файлов** зоны `Прога` (корень + три подпапки), без тематического справочника по семинарам.
- Решение: каталог в `Прога/docs/INDEX.md` (актуальные пути — с префиксами `01_`… и слоями `docs/`/`scripts/`).
- Вывод: при добавлении/удалении файлов обновлять таблицы в `INDEX.md`; для смысла и кода — `@` на конкретный `.txt` из папок материалов.

## 2026-04-17 — единый README для зоны Прога
- Контекст: нужна одна точка входа и явные правила для ИИ: контекст из `.txt` в «Материалы по ИАДу» и «Материалы по питону», не из «Оригиналы материалов» (кроме задач на конвертацию).
- Решение: добавлен `Прога/README.md`; строка в `tasks/project_map.md` обновлена.
- Вывод: при работе с курсом в Cursor ссылаться на `@Прога/README.md` или на конкретные `.txt`.

## 2026-04-17 — разделение выгрузок ИАД и Python
- Контекст: в одной папке «Материалы по ИАДу» смешивались конвертированные семинары ИАД (`Sem …`) и Python (`Seminar…`).
- Решение: создана `Прога/Материалы по питону/`; все `Seminar*.txt` перенесены туда; в `convert_materials.py` маршрутизация по префиксу имени файла; `Оригиналы материалов` не менялись.
- Вывод: при новых файлах соблюдать префиксы `Sem ` vs `Seminar`; скрипт кладёт выгрузку в нужную папку автоматически.

## 2026-04-17 — запуск системы
- Контекст: нужен агент структуры с «самообучением» без постоянной правки `.mdc`.
- Решение: введены `tasks/project_map.md` и этот файл; правило `project_structure_steward.mdc` только задаёт роль и указывает сюда.
- Вывод: обновлять карту и память после значимых сессий по структуре; общие уроки по ИИ — по-прежнему в `tasks/lessons.md`.
